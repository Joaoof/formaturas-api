services:
  api:
    image: formaturasflow-api:local
    build:
      context: ..
      dockerfile: Dockerfile
    labels:
      com.centurylinklabs.watchtower.enable: "false"

  watchtower:
    profiles: ["disabled"]
